#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int numOfSubarrays(vector<int> &arr, int k, int threshold) {
        int sum = 0;
        int average = 0;
        int ans = 0;

        for (int l = 0; l < arr.size(); r++) {
            sum += arr[l + k - 1]
        }
        return ans;
    }
};
